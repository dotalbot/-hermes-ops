# Immutable reviewer capability fixture

This text exists only to test exact commit/path/byte authentication. A capability preflight may prove it was read and hashed correctly. It may not infer or cache a product PASS from these bytes.

Required fixture behavior: reject aliases, altered bytes, altered path, altered digest, altered attempt, altered target tree, and altered controller source.
